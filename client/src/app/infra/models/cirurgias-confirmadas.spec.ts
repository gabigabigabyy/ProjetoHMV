import { CirurgiasConfirmadas } from './cirurgias-confirmadas';

describe('CirurgiasConfirmadas', () => {
  it('should create an instance', () => {
    expect(new CirurgiasConfirmadas()).toBeTruthy();
  });
});
