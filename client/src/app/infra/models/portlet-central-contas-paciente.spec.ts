import { PortletCentralContasPaciente } from './portlet-central-contas-paciente';

describe('PortletCentralContasPaciente', () => {
  it('should create an instance', () => {
    expect(new PortletCentralContasPaciente()).toBeTruthy();
  });
});
