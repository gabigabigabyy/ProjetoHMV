import { ContasProcessamentoCentral } from './contas-processamento-central';

describe('ContasProcessamentoCentral', () => {
  it('should create an instance', () => {
    expect(new ContasProcessamentoCentral()).toBeTruthy();
  });
});
